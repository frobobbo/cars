apiVersion: v1
kind: Service
metadata:
  name: {{ include "cars.fullname" . }}
  labels:
    {{- include "cars.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "cars.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
